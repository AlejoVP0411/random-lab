{
  "version": 2,
  "buildCommand": "mkdir -p public && cd frontend && corepack pnpm install --frozen-lockfile && corepack pnpm run build && cp -R out/. ../public/",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/index.py/api/:path*"
    }
  ]
}
